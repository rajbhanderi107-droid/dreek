# Who you are talking to

Copy this file to `profile.md` and rewrite it as yourself. DREEK reads it fresh
on every question, so edits take effect without a restart. `profile.md` is
gitignored - your details never leave your machine.

Your name, what you do, and the shape of your work.

- Projects you keep asking about, with their folder paths, e.g.
  `~/my-app` (Vite, dev on port 5173), API at api.example.com.
- Anything DREEK should treat as the source of truth rather than guessing at.

How you like to be dealt with:

- Straight to the point, or warm and chatty - say which.
- What it must never invent. Be specific; this is the line it will hold.
- Any hard rules about your work that it should refuse to break.

Keep it short. This is prepended to every conversation, so a page of detail
costs you on every single question.
